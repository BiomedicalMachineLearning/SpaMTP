% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MZAnnotation.R
\name{getRefinedAnnotations}
\alias{getRefinedAnnotations}
\title{Used to subset dataset to only include annotations that have n number of enteries
\itemize{
\item (i.e. some peaks can have multiple annotations. Peaks which have above n number of annotations assigned will be removed from Seurat Object)
}}
\usage{
getRefinedAnnotations(obj, assay = "Spatial", n = 1)
}
\arguments{
\item{obj}{Seurat object needing annotation refinement. This object must have annotations present in 'obj\verb{[[assay]]@meta.data}'}

\item{assay}{Character string defining the Seurat object assay where the annotation data is stored (default = "Spatial").}

\item{n}{Integer defining the number of enteries an annotation can have assigned. Any higher counts will be removed (default = 1).}
}
\value{
Refined Seurat object that only contains annotated mz values that have n number of annotations assigned (per mz value)
}
\description{
Used to subset dataset to only include annotations that have n number of enteries
\itemize{
\item (i.e. some peaks can have multiple annotations. Peaks which have above n number of annotations assigned will be removed from Seurat Object)
}
}
\examples{
#HMDB_db <- load("data/HMDB_1_names.rds")
AnnotatedSeuratObj <- AnnotateSeuratMALDI(SeuratObj, HMDB_db)

getRefinedAnnotations(AnnotatedSeuratObj, n = 2)
}
