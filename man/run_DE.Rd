% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DifferentialMZPeaks.R
\name{run_DE}
\alias{run_DE}
\title{Runs EdgeR analysis for pooled data
- This function is used by run_edgeR_annotations()}
\usage{
run_DE(
  pooled_data,
  seurat_data,
  ident,
  output_dir,
  run_name,
  n,
  logFC_threshold,
  annotation.column,
  assay,
  verbose = TRUE
)
}
\arguments{
\item{pooled_data}{A SingleCellExperiment object which contains the pooled pseudo-replicate data.}

\item{seurat_data}{A Seurat object containing the merged Xenium data being analysed (this is subset).}

\item{ident}{A character string defining the ident column to perform differential expression analysis against.}

\item{output_dir}{A character string defining the ident column to perform differential expression analysis against.}

\item{run_name}{A character string defining the title of this DE analysis (will be used when saving DEPs to .csv file).}

\item{n}{An integer that defines the number of pseudo-replicates per sample (default = 3).}

\item{logFC_threshold}{A numeric value indicating the logFC threshold to use for defining significant genes (default = 1.2).}

\item{annotation.column}{Character string defining the column where annotation information is stored in the assay metadata. This requires AnnotateSeuratMALDI() to be run where the default column to store annotations is "all_IsomerNames" (default = "None").}

\item{assay}{A character string defining the assay where the mz count data and annotations are stored (default = "Spatial").}

\item{verbose}{Boolean indicating whether to show the message. If TRUE the message will be show, else the messsage will be suppressed (default = TRUE).}
}
\value{
A list which contains the relative output requested by the "to_return" variable
}
\description{
Runs EdgeR analysis for pooled data
- This function is used by run_edgeR_annotations()
}
\examples{
# pooled_obj <- run_pooling(SeuratObj, "sample", n = 3)
# run_DE(pooled_obj, SeuratObj, "sample", "~/Documents/DE_output/", "run_1", n = 3, logFC_threshold = 1.2, annotation.column = "all_IsomerNames", assay = "Spatial")
}
