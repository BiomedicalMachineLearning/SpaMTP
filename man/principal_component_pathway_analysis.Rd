% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PathwayAnalysis.R
\name{principal_component_pathway_analysis}
\alias{principal_component_pathway_analysis}
\title{PCA driven pathway analaysis}
\usage{
principal_component_pathway_analysis(
  seurat,
  path = getwd(),
  ppm_error = NULL,
  ion_mode = NULL,
  tof_resolution = 30000,
  num_retained_component = NULL,
  variance_explained_threshold = 0.9,
  resampling_factor = 2,
  p_val_threshold = 0.05,
  byrow = T,
  assay = "SPM",
  slot = "counts",
  verbose = TRUE
)
}
\arguments{
\item{seurat}{is a Seurat object that contains spatial metabolic information}

\item{path}{is the output path for the visualization.}

\item{ppm_error}{is the parts-per-million error tolerance of matching m/z value with potential metabolites}

\item{ion_mode}{is only needed when ppm_error is not specified, goes to function estimate_mz_resolution_error will be used to access the ppm_error}

\item{tof_resolution}{is the tof resolution of the instrument used for MALDI run, calculated by ion (ion mass,m/z)/(Full width at half height)}

\item{num_retained_component}{is an integer value to indicated preferred number of PCs to retain}

\item{variance_explained_threshold}{Numeric value defining the explained variance threshold (default = 0.9).}

\item{resampling_factor}{is a numerical value >0, indicate how you want to resample the size of roginal matrix}

\item{p_val_threshold}{is the p value threshold for pathways to be significant}

\item{byrow}{is a boolean to indicates whether each column of the matrix is built byrow or bycol.}

\item{assay}{Character string defining the SpaMTP assay to extract intensity values from (default = "SPM").}

\item{slot}{Character string defining the assay slot contatin ght intesity values (default = "counts").}

\item{verbose}{Boolean indicating whether to show the message. If TRUE the message will be show, else the message will be suppressed (default = TRUE).}
}
\value{
PCA's and pathway_enrichment_pc is the pathway enrichment results for each PC
}
\description{
PCA driven pathway analaysis
}
\examples{
#principal_component_pathway_analysis(mass_matrix = readRDS("~/mass_matrix.rds")[,1:150],width = 912,height = 853,ppm_error = NULL,ion_mode = "positive",tof_resolution = 30000,input_mz = NULL,num_retained_component = NULL,variance_explained_threshold = 0.9,resampling_factor = 2,p_val_threshold = 0.05)
}
